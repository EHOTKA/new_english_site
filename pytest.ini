[pytest]
minversion = 7.0
testpaths = tests
pythonpath = .
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
markers =
    unit: pure logic, no database and no network (runs anywhere)
    integration: requires the PostgreSQL container from docker/docker-compose.yml
log_cli = false
